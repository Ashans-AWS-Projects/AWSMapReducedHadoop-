--In Local mode with input file on Hadoop instance:
a = LOAD 'C:/Users/ash47/Downloads/ECE4150-Lab5/ECE4150-Lab5/TestSets_Lab5/examples/book.txt' USING PIGSTORAGE(',') as (lines:chararray);

--In MapReduce mode with input file on HDFS:
--a = LOAD '/user/ashan/book.txt' as (lines:chararray);


b = FOREACH a GENERATE FLATTEN(TOKENIZE(lines)) as word;
c = GROUP b by word;
d = FOREACH c GENERATE group, COUNT(b);
store d into '/user/ashan/pig_wordcount_aws';


